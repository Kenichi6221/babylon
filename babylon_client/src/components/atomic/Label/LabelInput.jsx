import LabelAsPlaceHolder from '.';
import styled from 'styled-components';

export const LabelInput = styled(LabelAsPlaceHolder)`
  transform: translate(0.5rem, -0.5rem);
  font-size: 0.8rem;
`;

export default LabelInput;
