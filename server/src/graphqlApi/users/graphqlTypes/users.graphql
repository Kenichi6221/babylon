type User {
  id: ID
  name: String
  email: String
  password: String
  role: UserRole
}

input UserInput {
  name: String
  email: String!
  password: String!
  role: UserRole!
}

enum UserRole {
  SPEAKER
  ATTENDANT
}
